using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public static class PlayerState
{
    public static bool IsTopDown = false; // Informacja o trybie gry (true = top-down)
    public static Vector3 PlayerPosition = Vector3.zero; // Aktualna pozycja gracza
}
